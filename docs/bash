cd portfolio-site
git init
git remote add origin https://github.com/chelseakeel/aanalystportfolio
git add .
git commit -m "Initial commit with Power BI portfolio"
git push -u origin main
git add .
git commit -m "Update site"
git push origin main
git add --all
git commit -m "Initial commit"
git push -u origin main
git add .
git commit -m "Update site"
git push origin main

cd portfolio-site

# 2. See what files changed
git status index.html

# 3. Stage your changes
git add --all

# 4. Commit the changes
git commit -m "Update site"

# 5. Push to GitHub
git push origin main
